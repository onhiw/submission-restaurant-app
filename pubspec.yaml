name: submission_restaurant_app
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  shimmer: ^2.0.0
  cached_network_image: ^3.0.0
  google_fonts: ^2.1.0
  dio: ^4.0.0
  flutter_bloc: ^7.0.1
  fluttertoast: ^8.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.9.0

flutter_icons:
  image_path: "assets/restaurant_logo.png"
  android: true
  ios: true

flutter:

  uses-material-design: true

  assets:
    - assets/

